/* input 5 number with calculation 3 multiplication,
   10 addition , 2 division, 5 subtraction */
   function mindGame(number) {
    const multiplication = number * 3;
    const addition = multiplication + 10;
    const division = addition / 2;
    const subtraction = division - 5;
    return subtraction;
}
// const result = mindGame(5);
// console.log(result);

/* input 50 number with calculation 3 multiplication,
   10 addition , 2 division, 5 subtraction */
function mindGame(number) {
    const multiplication = number * 3;
    const addition = multiplication + 10;
    const division = addition / 2;
    const subtraction = division - 5;
    return subtraction;
}
// const result2 = mindGame(50);
// console.log(result2);

/* input 33 number with calculation 3 multiplication,
   10 addition , 2 division, 5 subtraction */
   function mindGame(number) {
    const multiplication = number * 3;
    const addition = multiplication + 10;
    const division = addition / 2;
    const subtraction = division - 5;
    return subtraction;
}
// const result3 = mindGame(33);
// console.log(result3);

//===================== PROBLEM SOLVE 2 =====================

// 'phero' text length find even or odd 
function evenOdd(string) {
    if (string.length % 2 === 0) {
        return 'even'
    } else {
        return 'odd'
    }
}
// const result = evenOdd('phero');
// console.log(result);

// 'Batch7' text length find even or odd
function evenOdd(string) {
    if (string.length % 2 === 0) {
        return 'even'
    } else {
        return 'odd'
    }
}
// const result2 = evenOdd('Batch7');
// console.log(result2);


// 'chatgpt' text length find even or odd
function evenOdd(string) {
    if (string.length % 2 === 0) {
        return 'even'
    } else {
        return 'odd'
    }
}
// const result3 = evenOdd('chatgpt');
// console.log(result3);

//===================== PROBLEM SOLVE 3 =====================

// 
function isLGSeven(anyNumber) {
    const result = anyNumber - 7;
    if ( result >= 7) {
        const multiplication = anyNumber * 2;
        console.log(multiplication);
        // return multiplication
    } else {
        // return result;
        console.log(result)
    }
}
const rr = isLGSeven(6);
// console.log(rr);